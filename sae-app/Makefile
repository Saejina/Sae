all: install

install-server:
	@make -C server install

install-client:
	@make -C client install

install: install-server install-client


start-server:
	@make -C server start

start client:
	@make -C client start
